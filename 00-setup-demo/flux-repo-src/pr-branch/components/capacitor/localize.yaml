apiVersion: delivery.ocm.software/v1alpha1
kind: Localization
metadata:
  name: capacitor
  namespace: ocm-system
spec:
  interval: 1m
  sourceRef:
    kind: ComponentVersion
    name: capacitor
    resourceRef:
      name: manifests
      version: latest
  configRef:
    kind: ComponentVersion
    name: capacitor
    resourceRef:
      name: config
      version: latest
